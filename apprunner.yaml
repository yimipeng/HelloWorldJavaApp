version: 1.0
runtime: corretto11
build:
  commands:
    build:
      - wget https://github.com/aws-observability/aws-otel-java-instrumentation/releases/download/v1.9.0/aws-opentelemetry-agent.jar -P /app/aws-opentelemetry-agent.jar
      - mvn clean package
run:
  command: java -javaagent:/app/aws-opentelemetry-agent.jar -jar target/HelloWorldJavaApp-1.0-SNAPSHOT.jar -Xms256m .
  network:
    port: 8080
